لوحة تحكم (Dashboard): أشوف فيها كل طلبات الصيانة الحالية.

إضافة طلب: (اسم العميل، نوع المشكلة، درجة الأهمية: Low, Medium, High).

تحديث الحالة: الموظف يقدر يغير حالة الطلب من "Pending" إلى "In Progress" أو "Completed".

حذف الطلب: إذا كان الطلب ملغى.

جدول الـ Category (كهرباء، سباكة، تكييف).

جدول الـ MaintenanceRequest (الطلب نفسه).


1 Pages
 -- Dashboard For Admin 
 -- Request Page for admin and employee => add new request update stauts delete request 



2 Tables
 -- Category 
    Id
    CategoryType (smallInt) (enums)
 -- MaintenanceRequest 
    Id
    Status (smallInt) (enums)
    CustomerName => will be a table if i complete devlopment 
    Description
    CreatedAt
    categoryId (fk)


Each Category may has many Request



-- start with Upload library 

downoladd => swagger, swaggerGen, SwaggerUI , Auto Mapper,
 entity framework and entity framework design and entity framework tools

Add connection string into appsettings.json file => not in env you now  

test the asp.net default api (work good) 
set swagger configuration using Microsoft docs  


Adding Global Error Handling
Create AppError Enums (from errors)
Create AppEception For custom errors 


Create Structure

Maintenance Task Tracker
    APi Layer (Maintenance Task Tracker)/
        Controllers
    Core/
        DTos/
        Enums/
        Entities/
        Exceptions/
        Interfaces/
            Service/
            Repository/
        Reults/
    Repositories/
        Config/
            MaintenanceRequestConfig
        Date/
            AppDbContext
        Migrations/
        Reposotry(genrec for crud)
    Servcies/
        profies/
        cateforyProfile
    CategoryService



src/
 ├── api/             # إعدادات Axios والـ Interceptors
 ├── assets/          # الصور، الأيقونات، والملفات الثابتة
 ├── components/      # المكونات الصغيرة (Buttons, Inputs, Cards)
 ├── layouts/         # الهياكل العامة (Sidebar + Navbar Layout)
 ├── pages/           # الصفحات الكاملة (Dashboard, RequestList)
 ├── services/        # ملفات الـ API calls (maintenanceService.js)
 └── utils/           # دوال مساعدة (Format Date, Constants)


 AspNetUsersالجدول الأساسي للـ 
 Identity — تسجيل دخول وإنشاء حسابات
 AspNetRolesإدارة الأدوار (Admin, Technician, User...)
 AspNetUserRolesربط المستخدم بالـ 
 RolePeopleتخزين البيانات الشخصية (الاسم، رقم الهاتف...)
 RefreshTokensإدارة الجلسات وتجديد الـ 
 JWTMaintenanceRequestجوهر المشروع — طلبات الصيانة
 Categoryتصنيف طلبات الصيانة


 Admin Can Create Servcies Request 
    Name, Description, Price, categoryId
    Addmin Assign Request to employee =>  already exists 
    Get All Request => wiht Category Name => search with Service name and filter by categoryId

Client Can Ask For Request 
    already exists Description with userId

Employee can see one requests and change status if completed